name: CI

on:
  pull_request:
  push:
    branches: [main]

jobs:
  checks:
    name: Build, test, and API smoke check
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install dependencies
        run: npm install

      - name: Formatting check
        run: npm run format:check

      - name: Build
        run: npm run build

      - name: Tests
        run: npm test

      - name: API smoke test
        run: npm run api:smoke
