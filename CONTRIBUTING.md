# Contributing To OrbitPay Server

## Local Work

Run:

```powershell
node server.mjs
```

## Contribution Ideas

- Add `GET /funding-sessions/:id`.
- Add in-memory tests using Node test runner.
- Add Stellar address validation.
- Add Horizon watcher.
- Add webhook signature verification.

Keep API changes documented in `README.md`.

